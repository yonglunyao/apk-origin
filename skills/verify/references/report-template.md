# 报告模板

Agent S 汇总判定时按此结构输出 Markdown 报告。

**核心原则：**
- 对比类信息优先使用表格呈现，避免纯列表
- **报告仅包含本次分析应用的信息，禁止引入其他应用的对比或案例引用**
- 关键信号前置（P0 → P1），权重递减

---

# [应用名 (包名)] 跨渠道来源一致性核查报告

> 分析日期：YYYY-MM-DD | 分析引擎：verify（P0→P1 多 Agent 并行）

## 0. 结论摘要

| 项 | 值 |
|----|----|
| **判定** | **同源 / 疑似篡改 / 不同源(仿冒)** |
| **置信度** | **高 / 中 / 低** |
| 关键风险 | (有/无，列出最高的 1-3 条) |

> (一段自然语言结论，说清判了什么、为什么、主要依据是什么。如有关键风险，在此醒目说明。)

### 应用画像

**基本信息**：

| 属性 | AG (AppGallery) | GP (Google Play) |
|------|-----------------|-------------------|
| 应用名 | (label) | (label) |
| 包名 | (package) | (package) |
| 版本 | (versionName / versionCode) | (versionName / versionCode) |
| 大小 | (APK size) | (APK size / 合并后) |
| 应用类型 | 钱包 / 交易所 / DeFi / 工具 / 其他 | 同左或不同 |
| 技术框架 | Flutter / React Native / 原生 Android | 同左或不同 |
| DEX 文件 | (n) | (n) |
| Native .so | (n) | (n) |
| 静态权限声明 | (n) | (n) |

**签名证书**：

| 属性 | AG | GP |
|------|-----|-----|
| 签名者 | (证书 DN 摘要) | (证书 DN 摘要) |
| 状态 | 开发者原始密钥 / 华为重签名 / 其他 | Google App Signing / 开发者原始 / 其他 |

**开发者身份**（基于 Agent A + I 联网搜索）：

| 来源 | 开发者 | 可信度 |
|------|--------|--------|
| Google Play | (开发者名称) | ✅ 已验证 |
| Apple App Store | (Seller 名称) | ✅ DUNS 验证 |
| 官网/GitHub | (域名/组织) | 仅供参考 |
| 监管/牌照 | (MSB/FINTRAC/FINCEN 等) | (有效/过期/无) |

**风险指标**（基于 Agent I 网络情报）：(如无命中则写"未发现"）

| 类别 | 发现 |
|------|------|
| 执法行动 | (FBI 查封 / 无) |
| 诈骗投诉 | (数量 + 典型损失) |
| 用户评价 | (Trustpilot / 应用商店评分) |
| 安全事件 | (已知漏洞 / 数据泄露) |

### 证据矩阵

| # | 分析点 | 阶段 | 判定 | 关键发现 |
|---|--------|------|------|---------|
| B | 网络端点差异 | P0 | 无差异/可解释/红旗 | (一句话) |
| A | 开发者身份与证书 | P0 | 开发者已确认/双平台重签名/红旗 | (一句话) |
| C | Manifest 差异 | P0 | 无差异/可解释/红旗 | (一句话) |
| I | 网络情报与舆情 | P0 | 无异常/有负面情报 | (一句话) |
| D | 入口点注入 | P1 | 无差异/发现注入/红旗 | (一句话) |
| E | Native .so 差异 | P1 | 无差异/可解释/红旗 | (一句话) |
| F | 硬编码敏感值 | P1 | 无差异/渠道差异/红旗 | (一句话) |
| G | 安全机制对比 | P1 | 一致/机制被移除 | (一句话) |
| H | 核心安全逻辑 | P1 | 一致/发现篡改/不适用 | (一句话) |

## 1. 关键信号（P0）

### 1.1 网络端点差异（Agent B）

| 类别 | AG | GP | 差异 |
|------|-----|-----|------|
| URL 总数 | (n) | (n) | AG独有:(n), GP独有:(n) |
| IP 总数 | (n) | (n) | AG独有:(n), GP独有:(n) |

| AG 独有关键域名 | 类别 | 威胁评估 |
|-----------------|------|---------|
| (domain) | (官方/三方API/CDN/可疑) | (正常/可疑/红旗) |

| 检查项 | 结果 |
|--------|------|
| 域名 whois 异常 | (有/无) |
| 裸 IP / 动态域名 / 短链 | (有/无) |
| pastebin/telegram/google docs | (有/无) |
| 两包共享核心后端 | (是 / 否，列出) |
| **判定** | **无差异 / 可解释 / 红旗** |

> (解释)

### 1.2 开发者身份与证书（Agent A）

| 属性 | AG (AppGallery) | GP (Google Play) |
|------|-----------------|-------------------|
| 证书 DN | (dn) | (dn) |
| SHA-256 | (sha256) | (sha256) |
| 签名方案 | (v1/v2/v3) | (v1/v2/v3) |
| 验证状态 | (ok/fail) | (ok/fail) |

| 检查项 | 结果 |
|--------|------|
| 是否 Google App Signing | (是/否) |
| 是否华为平台重签名 | (是/否) |
| 已验证开发者来源 | (Google Play / App Store / 官网 / 无) |
| AG 与已验证开发者是否一致 | (是/否/无基准/不适用-双平台重签名) |
| **判定** | **开发者已确认 / 一致 / 无官方基准 / 双平台重签名 / 红旗** |

> (解释)

### 1.3 网络情报与舆情（Agent I）

| 搜索词 | 引擎 | 关键发现 |
|--------|------|---------|
| (query) | (来源) | (摘要) |

| 情报维度 | 结果 |
|---------|------|
| 官方开发者身份 | (确认/未确认) |
| 执法行动 | (FBI查封/无) |
| 诈骗投诉 | (数量+典型损失/无) |
| 监管牌照 | (有效/过期/无) |
| 安全事件 | (已知漏洞/数据泄露/无) |
| 用户评价 | (评分+平台) |
| **综合风险等级** | **低 / 中 / 高** |

> (解释)

## 2. 结构分析（P1）

### 2.1 Manifest 差异（Agent C）

| 属性 | AG | GP | 差异评估 |
|------|-----|-----|---------|
| package | (包名) | (包名) | 一致 / 不同 |
| versionName | (v) | (v) | (是否合理) |
| versionCode | (n) | (n) | (是否合理) |
| minSdk | (n) | (n) | (是否合理) |
| targetSdk | (n) | (n) | (是否合理) |
| debuggable | (true/false) | (true/false) | (是否合理) |

| 类别 | AG 独有 | GP 独有 | 高危标记 |
|------|---------|---------|---------|
| 权限 | (列表) | (列表) | (有无 SMS/无障碍/悬浮窗等) |
| exported Activity | (列表) | (列表) | (有无异常) |
| exported Service | (列表) | (列表) | (有无异常) |
| exported Receiver | (列表) | (列表) | (有无异常) |
| exported Provider | (列表) | (列表) | (有无异常) |

| **判定** | **无差异 / 可解释 / 红旗** |

> (解释)

### 2.2 入口点注入（Agent D）

| 检查项 | AG | GP | 差异 |
|--------|-----|-----|------|
| Application 子类 | (类名) | (类名) | (一致/差异) |
| onCreate 注入点 | (有/无) | (有/无) | (详情) |
| attachBaseContext 注入点 | (有/无) | (有/无) | (详情) |
| 动态类加载 (DexClassLoader) | (有/无) | (有/无) | |
| 未知 .so 加载 | (有/无) | (有/无) | |

| 注入点位置 | 类型 | 代码片段 |
|-----------|------|---------|
| (类.方法) | (Service启动/广播注册/动态加载) | (code) |

| **判定** | **无差异 / 发现注入 / 红旗** |

> (解释)

### 2.3 Native .so 差异（Agent E）

| 类别 | AG | GP | 差异 |
|------|-----|-----|------|
| .so 总数 | (n) | (n) | AG独有:(n), GP独有:(n) |

| AG 独有 .so | 内部字符串 | 可疑导出函数 | 评估 |
|------------|-----------|-------------|------|
| (name.so) | (关键字符串) | (函数列表) | (正常/可疑) |

| 关键共享 .so | AG 导出数 | GP 导出数 | 差异函数 | 评估 |
|-------------|----------|----------|---------|------|
| (libcrypto等) | (n) | (n) | (列表) | (正常/可疑) |

| **判定** | **无差异 / 可解释 / 红旗** |

> (解释)

### 2.4 硬编码敏感值（Agent F）

| AG 独有敏感值 | 类型 | 出现位置 | 上下文分析 | 评估 |
|-------------|------|---------|-----------|------|
| (预览) | (API Key/JWT/私钥/凭证) | (类名) | (用途) | (渠道差异/可疑/红旗) |

| **判定** | **无差异 / 渠道差异 / 红旗** |

> (解释)

### 2.5 安全机制对比（Agent G）

| 安全机制 | AG | GP | 差异评估 |
|---------|-----|-----|---------|
| Root 检测 | (有/无) | (有/无) | (一致/AG缺失/GP缺失) |
| 模拟器检测 | (有/无) | (有/无) | (一致/AG缺失/GP缺失) |
| Frida 检测 | (有/无) | (有/无) | (一致/AG缺失/GP缺失) |
| SSL Pinning | (有/无) | (有/无) | (一致/AG缺失/GP缺失) |

| **判定** | **一致 / 机制被移除** |

> (解释)

### 2.6 核心安全逻辑（Agent H）

| 检查项 | AG | GP | 差异评估 |
|--------|-----|-----|---------|
| 助记词熵源 | (SecureRandom/Random/其他) | (SecureRandom/Random/其他) | (一致/降级) |
| 私钥存储方式 | (AndroidKeyStore/文件/SP) | (AndroidKeyStore/文件/SP) | (一致/简化) |
| 种子外传检测 | (有/无) | (有/无) | |

| 篡改点位置 | 描述 | 风险 |
|-----------|------|------|
| (类.方法) | (代码逻辑变化) | (高/中/低) |

| **判定** | **一致 / 发现篡改 / 不适用（说明原因）** |

> (解释)

## 4. 综合研判

当不同分析维度出现矛盾结论时（如证书红旗但代码一致）或存在需要解释的关键发现时，展开场景分析。

| 场景 | 描述 | 可能性 | 依据 |
|------|------|--------|------|
| 场景 1 | (描述) | 高/中/低 (xx%) | (依据) |
| 场景 2 | (描述) | 高/中/低 (xx%) | (依据) |

**综合判定**: (结论与核心推理链)

## 5. 建议措施

| 优先级 | 对象 | 措施 |
|--------|------|------|
| 紧急 | (委托人/用户/官方) | (具体行动) |
| 高 | | |
| 中 | | |

## 6. 残余风险

| 风险类别 | 描述 | 缓解建议 |
|---------|------|---------|
| 静态分析盲区 | 加密 payload / 运行时解密 / 远程配置下发 | Frida 动态 hook 网络层 |
| (应用类型相关) | (Flutter/RN/交易所等特定风险) | (建议) |

## 7. 分析过程元数据

| 项目 | 值 |
|------|-----|
| AG apk_extract.py 输出 | (路径) |
| GP apk_extract.py 输出 | (路径) |
| jadx 反编译路径 | (路径) |
| P0 / P1 Agent 耗时 | (分钟) |
| 网络情报搜索次数 | (n) |
| 总分析耗时 | (分钟) |
| 决策规则触发 | (哪条规则) |

## 附录A：分析方法

本报告采用 **verify** 多 Agent 并行编排框架，Phase 0 jadx CLI 统一反编译后供全部 Agent 共享读取，P0→P1 逐层递进。

### 分析阶段

| 阶段 | Agent | 职责 | 工具 |
|------|-------|------|------|
| P0 | B / A / C / I | 网络端点差异 / 开发者身份与证书 / Manifest 差异 / 网络情报与舆情 | apk_extract.py、aapt、Google Search |
| P1 | D / E / F / G / H | 入口点注入 / .so / 敏感值 / 安全机制 / 核心逻辑 | grep（共享 jadx 输出）、readelf、strings |
| S | S | 全量输出汇总 + 决策规则判定 | 报告模板 |

### 分析覆盖范围

| 覆盖层 | 内容 | 扫描方式 |
|--------|------|---------|
| DEX (Java/Kotlin) | URL、IP、敏感值（8 种模式） | apk_extract.py 二进制扫描 |
| Native .so | 文件存在性、内部 URL/IP/敏感值、符号表 | apk_extract.py + readelf/nm |
| Manifest | 权限、四大组件、元信息 | aapt 解析 |
| Assets | JS bundle（.bundle/.jsbundle/.hbc）、配置文件 | apk_extract.py 二进制扫描 |
| 开发者身份 | GP/App Store 企业验证 + GitHub/官网 | Google Search 多源交叉验证 |
| 网络舆情 | 执法行动、诈骗投诉、监管牌照、安全事件、用户评价 | Google Search（优先英文）|
| 加密货币专项 | scam/fraud/pig butchering/rug pull/FBI seizure/MSB license | Google Search 舆情专项 |

### 决策规则

| 条件 | 判定 | 置信度 |
|------|------|--------|
| 任一 P0 Agent 红旗 | 不同源/仿冒 | 高 |
| P0 全通过，任一 P1 Agent 红旗 | 疑似篡改 | 中 |
| 全部通过（含合理渠道差异） | 同源 | 高 |
| 金融应用 Agent I 发现执法查封 | 不同源/仿冒 | 高（独立触发） |

### 盲区声明

静态分析无法检测加密 payload、运行时解密、远程配置下发后门。Flutter 应用核心逻辑编译在 `libapp.so` 中（Dart AOT），DEX 层仅含引擎壳。React Native 应用 JS bundle 可能通过 CodePush 热更新覆盖静态分析结果。建议配合 Frida 动态 hook 网络层做补充验证。
